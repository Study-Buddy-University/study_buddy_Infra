# Database Configuration
POSTGRES_DB=studybuddy
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here

# Ollama Configuration (Docker Ollama - Linux/Mac)
# OLLAMA_BASE_URL defaults to http://ollama:11434 (Docker Ollama container)
# For Windows native Ollama, use .env.example.windows instead
OLLAMA_MODEL=llama3-groq-tool-use:8b
OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# GPU Configuration
# For NVIDIA: docker compose -f docker-compose.yml -f docker-compose.nvidia.yml up -d
# For AMD: docker compose -f docker-compose.yml -f docker-compose.amd.yml up -d
# For CPU: docker compose up -d
GPU_TYPE=nvidia
GPU_COUNT=all

# Backend Configuration
API_BASE_URL=http://localhost:8001

# Optional: Add API keys if needed
# OPENAI_API_KEY=your_key_here
